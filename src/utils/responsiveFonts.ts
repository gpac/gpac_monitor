/**
 * Responsive Font Size Utilities
 * Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
 */

/**
 * Metric value font size (main numbers)
 * Mobile: text-xs (12px) → Tablet: md:text-sm (14px) → Desktop: lg:text-base (16px)
 */
export const metricValueFont = 'text-xs md:text-sm lg:text-base';

/**
 * Metric label font size (stat names)
 * Mobile: text-[10px] → Tablet: md:text-xs (12px) → Desktop: lg:text-sm (14px)
 */
export const metricLabelFont = 'text-[10px] md:text-xs lg:text-sm';

/**
 * Technical details/small text font size
 * Mobile: text-[9px] → Tablet: md:text-[10px] → Desktop: lg:text-xs (12px)
 */
export const technicalDetailsFont = 'text-[9px] md:text-[10px] lg:text-xs';

/**
 * Header/name font size (PID names)
 * Mobile: text-sm (14px) → Tablet: md:text-base (16px) → Desktop: lg:text-lg (18px)
 */
export const headerFont = 'text-sm md:text-base lg:text-lg';

/**
 * Codec/format identifier font size
 * Mobile: text-[10px] → Tablet: md:text-xs (12px) → Desktop: lg:text-sm (14px)
 */
export const formatIdentifierFont = 'text-[10px] md:text-xs lg:text-sm';

/**
 * Small badge text font size
 * Mobile: text-[10px] → Tablet: md:text-xs (12px) → Desktop: lg:text-sm (14px)
 */
export const badgeFont = 'text-[10px] md:text-xs lg:text-sm';
